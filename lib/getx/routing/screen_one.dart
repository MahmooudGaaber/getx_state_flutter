import 'package:flutter/cupertino.dart';

class RoutingScreenOne extends StatelessWidget {
  const RoutingScreenOne({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
